// Middleware disabled for now - will implement custom locale detection
export function middleware() {
  // No middleware - using root-level pages with manual locale handling
}

export const config = {
  matcher: []
}